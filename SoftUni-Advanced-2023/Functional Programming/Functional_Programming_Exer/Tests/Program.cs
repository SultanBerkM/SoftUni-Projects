using System;
using System.Collections.Generic;
using System.Linq;

namespace Tests
{
    class Program
    {
        static void Main(string[] args)
        {
            List<string> names = new List<string>{ "Berk", "Sanchez", "Ian" };
            //string g = string.Empty;
            foreach (var item in names)
            {
                int i = item.FindIndex
            }
        }
    }
}
